{
  "name": "auth.smcdevs.support",
  "version": "1.0.0",
  "description": "Authorization service for SMCDEVS",
  "main": "src/app.ts",
  "scripts": {
    "build": "esbuild src/app.ts --bundle --minify --sourcemap --platform=node --target=es6 --outfile=app.js"
  },
  "author": [
    {
      "name": "Park Min Jae"
    }
  ],
  "license": "MIT ",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.97",
    "@types/node": "^17.0.38",
    "esbuild": "^0.15.8"
  },
  "dependencies": {
    "amazon-cognito-identity-js": "^5.2.9",
    "aws-lambda": "^1.0.7",
    "aws-sdk": "^2.1145.0",
    "ts-node": "^10.8.0"
  }
}
